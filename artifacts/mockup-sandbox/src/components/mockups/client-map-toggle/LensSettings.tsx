/** Map appearance — contrast / saturation / brightness, live over any lens. */
import { MapLenses } from "./MapLenses";

export function LensSettings() {
  return <MapLenses initial="radar" openAppearance />;
}
