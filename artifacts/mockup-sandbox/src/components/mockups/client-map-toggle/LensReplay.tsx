/** What actually happened today — the same lens dial, opened on the "replay" lens. */
import { MapLenses } from "./MapLenses";

export function LensReplay() {
  return <MapLenses initial="replay" />;
}
