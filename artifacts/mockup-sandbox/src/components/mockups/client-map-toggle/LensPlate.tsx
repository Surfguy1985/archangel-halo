/** Every unit's place in the turn — the same lens dial, opened on the "plate" lens. */
import { MapLenses } from "./MapLenses";

export function LensPlate() {
  return <MapLenses initial="plate" />;
}
