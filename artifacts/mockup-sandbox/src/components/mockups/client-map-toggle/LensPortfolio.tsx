/** All properties at once — the same lens dial, opened on the "portfolio" lens. */
import { MapLenses } from "./MapLenses";

export function LensPortfolio() {
  return <MapLenses initial="portfolio" />;
}
