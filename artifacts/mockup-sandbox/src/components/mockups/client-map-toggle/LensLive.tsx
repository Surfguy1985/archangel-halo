/** Who is on my property right now — the same lens dial, opened on the "radar" lens. */
import { MapLenses } from "./MapLenses";

export function LensLive() {
  return <MapLenses initial="radar" />;
}
