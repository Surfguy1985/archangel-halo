{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "seed:demo": "pnpm --filter @workspace/api-server seed:demo",
    "seed:live": "pnpm --filter @workspace/api-server seed:live",
    "seed:client-board": "pnpm --filter @workspace/api-server seed:client-board",
    "codegen:check": "pnpm --filter @workspace/api-spec run codegen:check"
  },
  "private": true,
  "dependencies": {
    "@replit/connectors-sdk": "^0.4.1"
  },
  "devDependencies": {
    "prettier": "^3.9.4",
    "puppeteer-core": "^25.5.0",
    "typescript": "~5.9.3",
    "vitest": "^4.1.10"
  }
}
